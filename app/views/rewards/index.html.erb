<div class="text-left text-[16px] sm:text-[20px] font-bold py-2">
  今までのごほうび
</div>
<div>
  <% @rewards.each do |reward| %>
    <div class="rounded-lg w-full md:w-200 h-15 border md:ml-30 text-[14px] sm:text-[18px] mt-5 p-2 md:flex items-center" style="background-color: #F2EDED;">
    <span><%= "目標： #{reward.required_points}ポイント" %></span></br>
      <span class="md:ml-20"><%= "ごほうび： #{reward.content}" %></span>
    </div>
  <% end %>
</div>

<div class="flex justify-center"><%= paginate @rewards %></div>