<div class="text-left text-[20px] font-bold py-2">
  克服ポイントカード
</div>
<p class="text-left">目標ポイント数と、達成時のごほうびを設定しましょう。</p>
<span class="text-left">＊現在のポイント数は</span>
<span class="rounded-lg border py-1 px-2" style="background-color: #F2EDED;"><%= "#{@user.point}" %></span>
<span>ポイントです。</span>

<div class="mt-15">
  <%= form_with model: @reward, url: user_rewards_path(current_user) do |f| %>
    <div class="flex items-center ml-5">
      <p>目標</p>
      <%= f.text_field :required_points, required: true, style: "font-size: 20px; width: 80px; height: 60px; text-align: center;", placeholder: "(例)15", class: "ml-5 mb-4 bg-gray-300" %>
      <p class="ml-5">ポイント</p>
    </div>
    <div class="mt-10 flex justify-center">
      <div class="w-2/3">
        <p>ごほうび</p>
        <%= f.text_area :content, required: true, style: "font-size: 18px; width: 100%;", placeholder: "(例)好きなお店に連れていく。", class: "mb-4 bg-gray-300 p-2" %>
      </div>
    </div>
    <div class="flex justify-center mt-10">
      <%= f.submit "設定", class: "btn btn-primary cursor-pointer rounded-lg border mt-1 py-2 px-4", style: "background-color: #F2EDED;" %>
    </div>
    <div class="flex justify-center mt-10 pb-20">
      <div class="flex justify-between">
        <div class="text-[20px]">
          <%= link_to "今までのこほうび", user_rewards_path(current_user.id) %>
        </div>
        <div class="text-[20px] ml-60">
          <%= link_to "克服ポイントとは？", others_point_explanation_path %>
        </div>
      </div>
    </div>
  <% end %>
</div>
