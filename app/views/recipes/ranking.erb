<div class="text-left text-[16px] sm:text-[20px] font-bold py-2">
  <p>人気レシピ</p>
  <p class="text-[10px] sm:text-[16px] mb-2 sm:mb-5">※獲得したお気に入り数に応じてランキング形式で表示しています。</p>
</div>
<div class="flex">
  <div class="flex justify-center grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-2 sm:gap-8 w-230 mb-3">
    <% @recipe_ranks.each_with_index do |recipe, index| %>
      <div>
        <span class="font-bold"><%= index + 1 %>位</span>
        <%= render recipe %>
      </div>
    <% end %>
  </div>
</div>