<div class="text-left text-[20px] font-bold py-2">
  <p>パスワードをお忘れの方へ</p>
</div>
<p>登録しているメールアドレスを入力してください。</p>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field mt-5 text-[20px]">
    <div class= "flex justify-center mr-100">
      <%= f.label :email, class: "hidden" %><br />
    </div>
    <div class= "flex justify-center">
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "bg-gray-300 w-140 h-10 mt-3 p-2",  placeholder: "メールアドレス" %>
    </div>
  </div>

  <div class="actions flex justify-center mt-10">
    <%= f.submit "送信", class: "border rounded-lg px-3 py-1", style: "background-color: #F2EDED;" %>
  </div>
<% end %>

<div class="actions flex justify-center mt-10">
  <%= link_to "ログイン画面へ", new_session_path(resource_name) %>
  <span>／</span>
  <%= link_to "アカウントの新規登録はこちら", new_registration_path(resource_name) %>
</div>
