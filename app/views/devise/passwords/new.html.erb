<div class="text-left text-[16px] sm:text-[20px] font-bold py-2">
  <p>パスワードをお忘れの方へ</p>
</div>
<p class="text-[12px] sm:text-[16px]">登録しているメールアドレスを入力してください。</p>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field md:mt-5 text-[20px]">
    <div class= "flex justify-center mr-100">
      <%= f.label :email, class: "hidden" %><br />
    </div>
    <div class= "flex justify-center">
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "text-[12px] sm:text-[16px] bg-gray-300 w-140 md:h-10 md:mt-3 p-2",  placeholder: "メールアドレス" %>
    </div>
  </div>

  <div class="actions flex justify-center mt-5 md:mt-10">
    <%= f.submit "送信", class: "text-[12px] sm:text-[16px] border rounded-lg px-3 py-1", style: "background-color: #F2EDED;" %>
  </div>
<% end %>

<div class="actions flex justify-center mt-5 md:mt-10 text-[12px] sm:text-[16px]">
  <%= link_to "ログイン画面へ", new_session_path(resource_name) %>
  <span>／</span>
  <%= link_to "アカウントの新規登録はこちら", new_registration_path(resource_name) %>
</div>
